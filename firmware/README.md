# Family Orb Firmware

This folder contains the Arduino firmware for the Family Orb prototype.

The main file is `family_orb.ino`.

The current firmware supports local signal display and does not transmit messages over a network.
